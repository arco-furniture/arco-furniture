import { createTheme } from '@mui/material'

export const profileButtonTheme = createTheme({
  typography: {
    button: {
      fontSize: 12,
      fontWeight: '400',
      textTransform: 'capitalize',
      letterSpacing: '0.01em',
      border: '1px solid #E0E0E0',
    },
  },
})
