import InputDefault from '../../ui/InputDefault'
import InputPassword from '../../ui/InputPassword'
import { render } from 'enzyme'
import { expect } from '@jest/globals'

describe('Input component tests', () => {
  // const propsDefault = {
  //   label: 'Имя',
  //   placeholder: 'Артем',
  //   type: 'text',
  //   error: jest.fn(),
  //   textError: jest.fn(),
  //   onChange: jest.fn(),
  // }
  // const componentDefault = render(<InputDefault {...propsDefault} />)
  // const componentPassword = render(<InputPassword />)
  // it('should render inputs ', () => {
  //   expect(componentDefault.find('input').length).toBe(1)
  // })
})
